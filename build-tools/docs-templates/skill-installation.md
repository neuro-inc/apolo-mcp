# Installation reference for an R&D job

This packaged skill reference embeds the canonical self-contained R&D runtime fragment.
The maintained source remains `build-tools/docs-templates/rnd-runtime.md`.

Before configuring unattended `full` operation, use the
{setup_skill_reference}.

## R&D runtime and job configuration

{rnd_runtime}
